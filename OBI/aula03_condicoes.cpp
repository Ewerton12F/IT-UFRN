#include <iostream>

using namespace std;

int main(){
    /* 
    IF
    if ( condição ) {
         comandos 
    }

    ELSE IF
    if([...]) {
        [...]
    } else if ([...]) {
        [...]
    } else if ([...]) {
        [...]
    }...

    ELSE
    if(a) {
        //[...]
    } else {
        //[...]
    }
    
    SWITCH-CASE
    switch(x) {
        case 1:
            // [...]
            break;
        case 2:-
            // [...]
            break;
        // outros casos
        default:
            // caso nenhum outro caso aconteça
            // [...]
    }

    OPERAÇÕES LÓGICAS
    a && b == a e b
    a || b == a ou b
    !a == não a
    a ^ b == a xor b

    COMPARADORES
    a <= b
    a >= b
    a == b
    a != b
    a < b
    a > b

    */
    return 0;
}
