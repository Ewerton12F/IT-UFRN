#include <iostream>
using namespace std;

int main(){
    int n;
    cin >> n;
    int notas[n];
    // Este comando cria tantas variáveis quanto ao número que o usuário digitou
    // Uma vez criado no programa, o array não pode mudar de tamanho
    return 0;
}
