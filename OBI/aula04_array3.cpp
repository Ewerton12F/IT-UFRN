#include <iostream>
using namespace std;

int main(){
    string s;
    cin >> s;

    s[0]; // -> primeiro caractere de s
    s[1]; // -> segundo caractere de s
    // [...]
    // Dica: s.size() → Retorna o tamanho da string
    return 0;
}
